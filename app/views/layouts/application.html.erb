<!DOCTYPE html>
<html>
  <head>
    <title>AutoCoverletter</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <% if Rails.env.development? %>
      <%= javascript_include_tag "hotwire-livereload", defer: true %>
    <% end %>
  </head>

  <body>
    <main class="container relative mx-m mt-8 px-4">
      <%= turbo_frame_tag "cover_letter_count", target: "_top" do %>
        <%= render CoverLetterCountComponent.new(
          cover_letter_count: (session[:cover_letters] || []).count
        ) %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
